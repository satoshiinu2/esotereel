[package]
name = "muscedit_core"
version = "0.1.0"
edition = "2024"

[dependencies]
muscedit_lib = { package = "muscedit_lib", path = "../lib" }
rkyv = { version = "0.7.10", features = ["validation"] }
