# Open Questions

このドキュメントは、Esotereelの現在の実装からでは判断できない仕様や、AIエージェントが実装時に判断が必要な箇所をまとめたものです。

## 1. アンドゥ/リドゥ機能

### 現状
- `CommandHistory` 構造は実装されている
- `command_to_history()` でCommandRequestをCommandHistoryに変換
- `handle_command_action()` でコマンド実行

### 不明点
- **GUI側のアンドゥ/リドゥ UI**: ユーザーインターフェースは実装されているか？
- **履歴スタック管理**: 履歴の保存、上限、管理ロジックは？
- **再実行ロジック**: CommandHistoryからの再実行メカニズムは？
- **状態ロールバック**: 失敗時の状態ロールバックはどうなるか？
- **履歴の永続化**: プロジェクト保存時の履歴の扱いは？

### 影響範囲
- `core/src/project/history.rs`
- `core/src/project/commands.rs`
- GUI側のアンドゥ/リドゥ実装

---

## 2. プロジェクト保存/読み込み

### 現状
- Project構造はserdeでシリアライズ可能
- `lib/src/project/save.rs` が存在するが実装は不明

### 不明点
- **ファイルフォーマット**: プロジェクトファイルのフォーマットは？
- **保存対象**: Project全体か、メタデータのみか？
- **メディアファイルのパス管理**: 絶対パスか相対パスか？
- **外部ファイルの扱い**: メディアファイルのコピー/リンクは？
- **バージョン管理**: ファイルフォーマットのバージョン管理は？
- **圧縮**: プロジェクトファイルの圧縮は？

### 影響範囲
- `lib/src/project/save.rs`
- `lib/src/project/project.rs`
- GUI側の保存/読み込みUI

---

## 3. エクスポート機能

### 現状
- レンダリング機能（`render_frame_offscreen`）は実装されている
- wgpuによるオフスクリーンレンダリングが可能

### 不明点
- **エンコーダー統合**: FFmpegエンコーダーの統合方法は？
- **エクスポート設定**: 解像度、コーデック、ビットレート等の設定は？
- **フォーマット選択**: サポートするエクスポートフォーマットは？
- **プログレス表示**: エクスポート進捗の表示方法は？
- **品質設定**: 品質と速度のバランス調整は？
- **オーディオ**: オーディオのエクスポートは？

### 影響範囲
- 新規エクスポートモジュール
- `lib/src/render/` の拡張
- GUI側のエクスポートUI

---

## 4. メディア管理

### 現状
- `InitStream` でビデオストリームを初期化
- `ResourceId` でリソースを管理
- `path_to_stream` マッピングでパスを管理

### 不明点
- **InitStreamとAddClipのタイミング**: メディアインポートとクリップ追加の順序は？
- **リロケーション**: プロジェクト移動時のパス修正は？
- **欠落ファイル**: メディアファイルが見つからない場合の処理は？
- **キャッシュ**: デコード済みフレームのキャッシュ戦略は？
- **メモリ管理**: 大きなファイルのメモリ管理は？
- **オーディオファイル**: オーディオファイルの取り扱いは？

### 影響範囲
- `lib/src/decode/`
- `core/src/state.rs`
- GUI側のメディアライブラリ

---

## 5. Script連携

### 現状
- `ClipData::Script` が定義されている
- Rhaiスクリプトエンジンが統合されている
- `ScriptStore` と `CompiledScript` が存在

### 不明点
- **評価タイミング**: スクリプトはいつ評価されるか？
- **Timeline生成**: `generated` TimelineIdの設定タイミングは？
- **パラメータ渡し**: ScriptParamsの具体的な使い方は？
- **エラーハンドリング**: スクリプトエラーの処理は？
- **デバッグ**: スクリプトのデバッグ方法は？
- **パフォーマンス**: スクリプト評価のパフォーマンス影響は？

### 影響範囲
- `lib/src/plugin/script/`
- `lib/src/project/clip.rs`
- レンダリングパイプライン

---

## 6. Nested Timeline

### 現状
- `CompositionRef` (Mirror/Independent) が定義されている
- `ClipData::Composite`, `Area2D`, `Area3D` が存在
- `Project::make_independent()` が実装されている

### 不明点
- **レンダリング実装**: ネストされたTimelineのレンダリングは？
- **Mirror参照**: 複数クリップからのMirror参照の扱いは？
- **Independent化のトリガー**: いつIndependent化するか？
- **深さ制限**: `MAX_NESTED_DEPTH` の適用範囲は？
- **変更伝播**: ネストされたTimelineの変更伝播は？
- **パフォーマンス**: 深いネストのパフォーマンス影響は？

### 影響範囲
- `lib/src/project/clip.rs`
- `lib/src/project/project.rs`
- `lib/src/render/`

---

## 7. オーディオ処理

### 現状
- `ClipData::Audio` が定義されている
- FFmpegはオーディオデコードをサポート

### 不明点
- **実装有無**: オーディオ処理は実装されているか？
- **波形表示**: オーディオ波形の表示は？
- **オーディオエフェクト**: オーディオエフェクトの適用は？
- **ミキシング**: 複数オーディオのミキシングは？
- **同期**: オーディオとビデオの同期は？
- **エクスポート**: オーディオのエクスポートは？

### 影響範囲
- 新規オーディオモジュール
- `lib/src/decode/` の拡張
- GUI側のオーディオUI

---

## 8. GUI側の実装詳細

### 現状
- C++ FFIラッパーが存在
- Qt6ベースのGUI

### 不明点
- **オフライン操作**: ネットワーク切断時のGUI操作の扱いは？
- **リアルタイムプレビュー**: プレビュー用の専用通信チャネルは？
- **ドラッグ&ドロップ**: ドラッグ操作の詳細実装は？
- **ショートカット**: キーボードショートカットの実装は？
- **テーマ**: ダークモード等のテーマ対応は？
- **アクセシビリティ**: アクセシビリティ対応は？

### 影響範囲
- `gui/src/`
- `guihlp/src/ffi/`

---

## 9. ネットワークと競合処理

### 現状
- TCP/IPベースの通信
- 複数クライアント接続をサポート

### 不明点
- **複数クライアントの編集競合**: 同時編集の競合処理は？
- **ロック戦略**: 楽観的ロック/悲観的ロックの使用は？
- **競合解決**: 競合検出と解決メカニズムは？
- **切断回復**: ネットワーク切断時の回復は？
- **バッファリング**: 通信バッファのサイズと戦略は？
- **セキュリティ**: 認証と暗号化の実装は？

### 影響範囲
- `core/src/network.rs`
- `guihlp/src/network.rs`
- プロトコル設計

---

## 10. パフォーマンスとスケーラビリティ

### 現状
- ChunkIndexによる範囲検索の最適化
- 増分更新によるネットワーク効率化

### 不明点
- **大量クリップ**: 1000+クリップ時のパフォーマンスは？
- **大規模プロジェクト**: プロジェクトサイズの上限は？
- **メモリ使用**: メモリ使用量の最適化戦略は？
- **GPUメモリ**: GPUメモリの管理は？
- **並列処理**: 並列レンダリングの実装は？
- **プロファイリング**: パフォーマンスプロファイリングツールは？

### 影響範囲
- 全体のアーキテクチャ
- `lib/src/project/chunk_index.rs`
- `lib/src/render/`

---

## 11. エラーハンドリング

### 現状
- `EsotereelError` と `EsotereelResult` が定義されている
- ログ出力が実装されている

### 不明点
- **GUIエラー表示**: ユーザーへのエラー表示方法は？
- **リカバリー**: エラーからのリカバリー戦略は？
- **ログレベル**: ログレベルの設定と管理は？
- **エラーレポート**: エラーレポートの収集は？
- **ユーザーフィードバック**: エラー時のユーザーガイダンスは？

### 影響範囲
- 全体のエラーハンドリング
- GUI側のエラーUI
- ログシステム

---

## 12. プラグインシステム

### 現状
- `PluginLoader` が実装されている
- ClipKind、ToolbarButton、Scriptの定義が可能

### 不明点
- **プラグインAPI**: プラグイン開発者向けAPIの詳細は？
- **セキュリティ**: プラグインのセキュリティサンドボックスは？
- **依存関係**: プラグイン間の依存関係管理は？
- **バージョニング**: プラグインのバージョン管理は？
- **配布**: プラグインの配布方法は？
- **ホットリロード**: ホットリロードの安全性は？

### 影響範囲
- `lib/src/plugin/`
- プラグイン開発ドキュメント

---

## 13. テスト

### 現状
- 一部の単体テストが存在

### 不明点
- **テストカバレッジ**: 目標テストカバレッジは？
- **統合テスト**: ネットワーク通信のテストは？
- **E2Eテスト**: エンドツーエンドテストの戦略は？
- **ベンチマーク**: パフォーマンスベンチマークは？
- **テストデータ**: テスト用メディアファイルの管理は？

### 影響範囲
- 全体のテスト戦略
- CI/CDパイプライン

---

## 14. ローカライゼーション

### 現状
- `esotereel_ja.ts` と `esotereel_en.ts` が存在

### 不明点
- **翻訳範囲**: 翻訳対象の範囲は？
- **更新プロセス**: 翻訳の更新プロセスは？
- **複数言語**: 2言語以上のサポートは？
- **フォント**: 多言語フォントの対応は？
- **日付/時刻**: ロケールに応じたフォーマットは？

### 影響範囲
- GUI側のローカライゼーション
- 翻訳ファイル管理

---

## 15. ビルドとデプロイ

### 現状
- CMake + Cargoのハイブリッドビルド
- Linux, Windows, macOS対応

### 不明点
- **インストーラー**: 各プラットフォームのインストーラーは？
- **自動更新**: 自動更新機能の実装は？
- **依存関係**: システム依存関係の管理は？
- **コンテナ化**: Docker等のコンテナ対応は？
- **配布**: 配布チャネルと戦略は？

### 影響範囲
- `CMakeLists.txt`
- `installer/`
- ビルドシステム

---

## 優先度分類

### 高優先度（実装ブロッカー）
1. アンドゥ/リドゥ機能の完全実装
2. プロジェクト保存/読み込み
3. エラーハンドリングの統一
4. GUI側のオフライン操作

### 中優先度（機能完全性）
5. エクスポート機能
6. メディア管理の改善
7. Script連携の詳細化
8. Nested Timelineのレンダリング

### 低優先度（将来拡張）
9. オーディオ処理
10. プラグインシステムの拡張
11. テストカバレッジ向上
12. ローカライゼーション拡張

---

## 実装時の判断基準

AIエージェントが実装時に判断が必要な場合：

1. **既存パターンに従う**: 同様の機能が既に実装されている場合、そのパターンを模倣する
2. **最小限の実装**: 不明な点は最小限の機能で実装し、後で拡張可能にする
3. **ログ出力**: 不明な挙動はログを出力してデバッグ可能にする
4. **エラーハンドリング**: 不明なエラーは適切に処理し、ユーザーに通知する
5. **ドキュメント更新**: 実装した機能はこのドキュメントを更新する
